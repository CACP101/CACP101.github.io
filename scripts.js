function animate(input) {
    document.getElementById(input).style.animation = "colorchange 20s 20s forward"
}
document.addEventListener("DOMContentLoaded", (event) => {
  console.log("DOM fully loaded and parsed");
});